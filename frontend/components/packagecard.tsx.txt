import { useRouter } from 'next/router';

export default function PackageCard({ package: pkg }: any) {
  const router = useRouter();

  return (
    <div className="bg-gray-800 rounded-xl p-6 border border-gray-700 hover:border-cyan-400 transition-all">
      <h3 className="text-xl font-bold mb-2">{pkg.name}</h3>
      <p className="text-gray-400 text-sm mb-4">{pkg.summary}</p>
      <p className="text-2xl font-bold text-cyan-400 mb-4">
        ${pkg.pricing.monthly_usd}/ay
      </p>
      <button
        onClick={() => router.push(/checkout?packageId=${pkg.id})}
        className="w-full bg-cyan-500 hover:bg-cyan-600 py-2 rounded font-semibold"
      >
        Hýzlý Baþlat
      </button>
    </div>
  );
}